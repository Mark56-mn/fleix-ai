export function getProblems(): string[] {
  return [
    'Logic: If all A are B and some B are C, what can be inferred?',
    'Math: Derive time complexity of binary search.',
    'Coding: Design an LRU cache in TypeScript.'
  ];
}
